# CLI To-Do App
A command-line to-do list app built with Python.
Todos are saved to a JSON file and persist between sessions.

## How to run
Navigate to the directory of this project and run in a terminal:

```
python todo_list.py
```